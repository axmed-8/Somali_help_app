# Copy this file to db_config.env and fill in your local MySQL credentials.
# db_config.env is gitignored and must never be committed.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=gurmad
DB_USER=root
DB_PASSWORD=your_password_here
