# Deployment / local env template (not used by python app.py unless you export these).
# On Render, set these in the dashboard or via render.yaml.

SECRET_KEY=change-me-in-production
GURMADNET_DB=json
